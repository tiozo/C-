#include<bits/stdc++.h>
using namespace std;

int main() {
    vector<int> a(1,0);
    for (auto &e:a) cout << "1\n";
    for (auto it = a.begin(); it!=a.end();++it) {
        cout << 1 << '\n';
    } 
    return 0;
}